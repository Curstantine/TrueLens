import type { Metadata } from "next";

export const metadata: Metadata = {
	title: "TrueLens - Home",
};

export default function Page() {
	return (
		<main className="">
			<span>home</span>
		</main>
	);
}
